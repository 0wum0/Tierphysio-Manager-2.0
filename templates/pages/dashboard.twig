{% extends 'layouts/base.twig' %}

{% block title %}Dashboard - {{ parent() }}{% endblock %}

{% block content %}
<!-- Welcome Section -->
<div class="mb-8">
    <h1 class="text-3xl font-heading font-bold text-gray-900 dark:text-white">
        {{ greeting }}, {{ user.first_name }}! 👋
    </h1>
    <p class="text-gray-600 dark:text-gray-400 mt-2">
        Hier ist Ihre Übersicht für {{ "now"|date("l, d. F Y") }}
    </p>
</div>

<!-- Statistics Cards -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
    <!-- Active Patients -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6 animate-in">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Aktive Patienten</p>
                <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">{{ stats.active_patients }}</p>
            </div>
            <div class="w-12 h-12 bg-purple-100 dark:bg-purple-900/30 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-purple-600 dark:text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                          d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                </svg>
            </div>
        </div>
        <div class="mt-4 flex items-center text-sm">
            <span class="text-green-600 dark:text-green-400 flex items-center">
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                +5%
            </span>
            <span class="text-gray-500 dark:text-gray-400 ml-2">vs. letzten Monat</span>
        </div>
    </div>
    
    <!-- Today's Appointments -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6 animate-in">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Heutige Termine</p>
                <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">{{ stats.today_appointments }}</p>
            </div>
            <div class="w-12 h-12 bg-blue-100 dark:bg-blue-900/30 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                          d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
            </div>
        </div>
        <div class="mt-4">
            <a href="/public/appointments.php" class="text-sm text-purple-600 dark:text-purple-400 hover:text-purple-700 dark:hover:text-purple-300">
                Alle Termine anzeigen →
            </a>
        </div>
    </div>
    
    <!-- Open Invoices -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6 animate-in">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Offene Rechnungen</p>
                <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">{{ stats.open_invoices }}</p>
                <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">{{ currency(stats.open_invoices_amount) }}</p>
            </div>
            <div class="w-12 h-12 bg-yellow-100 dark:bg-yellow-900/30 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-yellow-600 dark:text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                          d="M9 14L5 18m0 0l4 4m-4-4h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v9a2 2 0 002 2z"></path>
                </svg>
            </div>
        </div>
        <div class="mt-4">
            <a href="/public/invoices.php" class="text-sm text-purple-600 dark:text-purple-400 hover:text-purple-700 dark:hover:text-purple-300">
                Rechnungen verwalten →
            </a>
        </div>
    </div>
    
    <!-- Monthly Revenue -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6 animate-in">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Monatsumsatz</p>
                <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">{{ currency(stats.month_revenue) }}</p>
            </div>
            <div class="w-12 h-12 bg-green-100 dark:bg-green-900/30 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                          d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>
        </div>
        <div class="mt-4 flex items-center text-sm">
            <span class="text-green-600 dark:text-green-400 flex items-center">
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                </svg>
                +12%
            </span>
            <span class="text-gray-500 dark:text-gray-400 ml-2">vs. letzten Monat</span>
        </div>
    </div>
</div>

<!-- Main Content Grid -->
<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Left Column -->
    <div class="lg:col-span-2 space-y-6">
        <!-- Upcoming Appointments -->
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 animate-in">
            <div class="p-6 border-b border-gray-200 dark:border-gray-700">
                <h2 class="text-lg font-heading font-semibold text-gray-900 dark:text-white">Anstehende Termine</h2>
            </div>
            <div class="p-6">
                {% if appointments %}
                    <div class="space-y-4">
                        {% for appointment in appointments %}
                            <div class="flex items-center space-x-4 p-3 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                                <div class="flex-shrink-0">
                                    <div class="w-10 h-10 bg-purple-100 dark:bg-purple-900/30 rounded-full flex items-center justify-center">
                                        <span class="text-xs font-semibold text-purple-600 dark:text-purple-400">
                                            {{ appointment.appointment_date|date("d") }}
                                        </span>
                                    </div>
                                </div>
                                <div class="flex-1 min-w-0">
                                    <p class="text-sm font-medium text-gray-900 dark:text-white truncate">
                                        {{ appointment.patient_name }}
                                    </p>
                                    <p class="text-xs text-gray-500 dark:text-gray-400">
                                        {{ appointment.owner_first_name }} {{ appointment.owner_last_name }} • 
                                        {{ appointment.start_time|time_format }} - {{ appointment.end_time|time_format }}
                                    </p>
                                </div>
                                <div>
                                    <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium 
                                                 bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400">
                                        {{ appointment.type == 'initial' ? 'Ersttermin' : 'Folgetermin' }}
                                    </span>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <p class="text-gray-500 dark:text-gray-400 text-center py-8">
                        Keine anstehenden Termine
                    </p>
                {% endif %}
            </div>
        </div>
        
        <!-- Revenue Chart -->
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6 animate-in">
            <h2 class="text-lg font-heading font-semibold text-gray-900 dark:text-white mb-4">Umsatzentwicklung</h2>
            <canvas id="revenueChart" class="w-full h-64"></canvas>
        </div>
    </div>
    
    <!-- Right Column -->
    <div class="space-y-6">
        <!-- Recent Patients -->
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 animate-in">
            <div class="p-6 border-b border-gray-200 dark:border-gray-700">
                <h2 class="text-lg font-heading font-semibold text-gray-900 dark:text-white">Neue Patienten</h2>
            </div>
            <div class="p-6">
                {% if recentPatients %}
                    <div class="space-y-3">
                        {% for patient in recentPatients %}
                            <div class="flex items-center space-x-3">
                                <div class="flex-shrink-0">
                                    {% if patient.image %}
                                        <img src="{{ asset('uploads/' ~ patient.image) }}" alt="{{ patient.name }}" 
                                             class="w-10 h-10 rounded-full object-cover">
                                    {% else %}
                                        <div class="w-10 h-10 bg-gray-200 dark:bg-gray-700 rounded-full flex items-center justify-center">
                                            <svg class="w-5 h-5 text-gray-400" fill="currentColor" viewBox="0 0 24 24">
                                                <path d="M4.5 10.5C4.5 12.9853 6.51472 15 9 15C11.4853 15 13.5 12.9853 13.5 10.5C13.5 8.01472 11.4853 6 9 6C6.51472 6 4.5 8.01472 4.5 10.5Z"/>
                                            </svg>
                                        </div>
                                    {% endif %}
                                </div>
                                <div class="flex-1 min-w-0">
                                    <p class="text-sm font-medium text-gray-900 dark:text-white truncate">
                                        {{ patient.name }}
                                    </p>
                                    <p class="text-xs text-gray-500 dark:text-gray-400 truncate">
                                        {{ patient.owner_first_name }} {{ patient.owner_last_name }}
                                    </p>
                                </div>
                                <div>
                                    <span class="text-xs text-gray-500 dark:text-gray-400">
                                        {{ patient.species == 'dog' ? '🐕' : patient.species == 'cat' ? '🐈' : '🐾' }}
                                    </span>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <p class="text-gray-500 dark:text-gray-400 text-center py-4">
                        Keine neuen Patienten
                    </p>
                {% endif %}
            </div>
        </div>
        
        <!-- Quick Actions -->
        <div class="bg-gradient-to-br from-purple-600 to-indigo-600 rounded-xl shadow-sm p-6 text-white animate-in">
            <h2 class="text-lg font-heading font-semibold mb-4">Schnellzugriff</h2>
            <div class="space-y-3">
                <a href="/public/patients.php?action=new" 
                   class="flex items-center justify-between p-3 bg-white/20 rounded-lg hover:bg-white/30 transition-colors">
                    <span class="text-sm font-medium">Neuer Patient</span>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                </a>
                <a href="/public/appointments.php?action=new" 
                   class="flex items-center justify-between p-3 bg-white/20 rounded-lg hover:bg-white/30 transition-colors">
                    <span class="text-sm font-medium">Termin buchen</span>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                </a>
                <a href="/public/invoices.php?action=new" 
                   class="flex items-center justify-between p-3 bg-white/20 rounded-lg hover:bg-white/30 transition-colors">
                    <span class="text-sm font-medium">Rechnung erstellen</span>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                </a>
            </div>
        </div>
    </div>
</div>

<script>
    // Initialize Chart.js
    document.addEventListener('DOMContentLoaded', () => {
        const ctx = document.getElementById('revenueChart').getContext('2d');
        const isDark = document.documentElement.classList.contains('dark');
        
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mär', 'Apr', 'Mai', 'Jun', 'Jul', 'Aug', 'Sep', 'Okt', 'Nov', 'Dez'],
                datasets: [{
                    label: 'Umsatz',
                    data: [4200, 4800, 5200, 5600, 6100, 5800, 6400, 6800, 7200, 6900, 7500, 0],
                    borderColor: '#9b5de5',
                    backgroundColor: 'rgba(155, 93, 229, 0.1)',
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        callbacks: {
                            label: function(context) {
                                return '€ ' + context.parsed.y.toLocaleString('de-DE');
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            color: isDark ? '#9ca3af' : '#6b7280'
                        }
                    },
                    y: {
                        ticks: {
                            callback: function(value) {
                                return '€' + value.toLocaleString('de-DE');
                            },
                            color: isDark ? '#9ca3af' : '#6b7280'
                        },
                        grid: {
                            color: isDark ? 'rgba(55, 65, 81, 0.5)' : 'rgba(229, 231, 235, 0.5)'
                        }
                    }
                }
            }
        });
    });
</script>
{% endblock %}